{
  "private": true,
  "name": "patricialopes",
  "version": "1.0.2",
  "scripts": {
    "postinstall": "yarn global add gatsby-cli && cd web && yarn install"
  },
  "devDependencies": {
    "@sanity/cli": "^1.149.16",
    "lerna": "^3.22.1"
  }
}
